# Library versions pinned to match the training environment, so the serialized pipeline loads correctly
pandas==2.2.2
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
joblib==1.4.2
flask==3.1.1
gunicorn==23.0.0
